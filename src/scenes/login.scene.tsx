import { AppLayout } from '@/layouts';
import { LoginContainer } from '@/pods/login';

export const LoginScene: React.FC = () => {
  return (
    <AppLayout>
      <LoginContainer />
    </AppLayout>
  );
};
